# LMP specifications

Place LMP specification (`.md`) files here.

The main LMP v1 spec lives in the repo at:

- `../../specs/LMP v1 - Spec.md`

You can copy or symlink it here for the compiler repo's reference.
